import tadbcmc.data.enums.enemy as e
from ...config.defaults import DEFAULT_CONFIG






""" all these functions act on a single enemy and return it """
def white_gimmick(stats,config=DEFAULT_CONFIG):
    """ gives a white unit sage """
    #the only thing is sage right?
    do_sage = config["enemy"]["trait_gimmicks"]["sage"]
    print(do_sage)



















