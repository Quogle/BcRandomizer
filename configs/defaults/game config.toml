# Randomizer config for The Battle Cats

[general]
# ENTER RANDOMIZATION SEED BELOW (if not, a random one will be chosen)
seed = 0


################# GAMEPLAY CHANGES #################

[gameplay]
remove_metals = true                    # literally removes metals from the game, recommended
give_metals_new_trait = true            # gives ex metals a trait
use_metal_rework = true                 # uses my ex metal rebalance
remove_behemoths = true
use_behemoth_rebalance = true           # uses my ex behemoth rebalance
preserve_old_zombies = true             # swaps zkiller and witch so old zombies can still burrow (makes sorting cats weird)
preserve_alien_mags_main_chapters = true# buffs itf/cotc aliens if no longer alien or crystals removed
preserve_cotc_mags_sol_ul_events = true # buffs cotc aliens in sol and ul and events (like cosmic cyclone)
remove_itf_crystals = false             # removes itf crystals from the game (if false no new aliens can be in itf to make it not stupid)
remove_cotc_crystals = true             # removes cotc crystals from the game, recommended who likes these

[gameplay.unit_reworks]
rework_units = true                     # reworks ex metals and certain favoured units, theyre fun
courier = true                          # removes massive to stop it from killing an entire trait
jurassic = true
space = true
jumprope = true
hurricat = true
waitress = true
aku_researcher = true
backhoe = true
mint = true
punt = true
merc = true
capsule = true
racism_cow = true
reaper = true
million_dollar = true#still unsure on how Im doing this one
cop = true
paladin = true
cat_clan = true
verbena = true
hayabusa = true
moneko = true
neneko = true
summer_neneko = true
new_year_neneko = true                  
valentine_neneko = true
easter_neneko = true
cmoneko = true


################# CAT COMBO CHANGES #################

[catcombo]
RANDOMIZE_COMBOS = true
randomize_units = true                                  # randomize cat combo units
randomize_multipliers = true                            # randomize combo multiplier sizes (sm, M, L, XL, DOWN)
randomize_effects = true                                # randomize combo effects
max_uber_count = 1                                      # How many ubers/lr can be in a catcombo (1-5)
allow_legend_rares = false                              # Whether legend rares can be in combos (will use max_uber_count)

[catcombo.blacklist]                                    # units blacklisted from appearing in combos
collab = true                                           # dissallow units only obtained through collaborations
version_exclusive = true                                # dissallow units exclusive to non english versions of the game
unobtainable = true                                     # dissallow normally unobtainable units ( iron wall, conjures )
limited_event = true                                    # dissallow limited promotion units (such as capsule and gacha cat)

[catcombo.size]
CUSTOM_MULTIPLIER_WEIGHTS = true                        # If false, chooses between the 5 combo effect multipliers at equal chance  

# For each combo unit count (1–5), defines the weight of each effect size
# Higher value = higher chance of being selected
# Only used if CUSTOM_MULTIPLIER_WEIGHTS = true
1 = { SM = 45, M = 25, L = 5, XL = 0, DOWN = 20 }                  
2 = { SM = 35, M = 40, L = 10, XL = 0, DOWN = 15 }    
3 = { SM = 10, M = 45, L = 35, XL = 5, DOWN = 5 }    
4 = { SM = 0, M = 15, L = 65, XL = 15, DOWN = 5 }    
5 = { SM = 0, M = 5, L = 70, XL = 25, DOWN = 0 } 

preserve_combo_unit_count = true                        # Whether combos stay at their original unit count                            #
CUSTOM_UNIT_COUNT_WEIGHTS = false                       # If false, chooses between having 1-5 units at random

# Relative weights for choosing combo unit count (1–5)
# Higher value = higher chance of being selected        
# Only applied if CUSTOM_UNIT_COUNT_WEIGHTS = true
unit_count_weights = { "1" = 10, "2" = 25, "3" = 35, "4" = 20, "5" = 10 }


################# QUALITY OF LIFE CHANGES #################

[qol]
behemoth_cube_buff = true#
enigma_buff = true
weekday_specials = true                         # replaces weekday stages with special tf stages
weekend_lils = true                             # replaces weekend stage with lil ticket farming stages
xp_stage_item_farming = true#do I still even want this
shop_buff = true#
cheaper_items = true#
leadership_in_shop = true#
ototo_helper_in_shop = true#
cheap_catamin = true#
unit_sell_increase = true#                     # triples xp sell, doubles np sell
free_orb_removal = true#
guarantee_advent_drops = true#


[qol.stage_changes]
seasonals_in_sol = true#                         # adds monthly to sol
collabs_in_sol_advents = true#                   # adds a limited selection of collab units to certain sol stages and advents (also changes nenekos/brainwashed to be stage drops)
collab_tf_as_advents = true#                     # adds ^ true forms to advents (kills xp drop)
mission_collab_special_tf_drop = true   #not added        # overwrites advent collab tf to instead add the true forms as missions
buff_normal_catfruit_catamins = true#            # buffs catamin catfruit stages, 2seeds stage1, 1fruit stage2, 2fruit stage3 with gauranteed jubilee, it has occured to me now that Ive coded it that these are no longer in the game
split_normal_catfruit_stages = true#             # adds old third stages that always gives 2 fruit and jubilee
jubilee_always_epic = true#                      # jubilee always drops epic fruit
gstrange_buff = true#                            # first stage drops a seed, extra stage seed drop increased to 4
growing_aku_buff = true#                         # buffs third and fourth growing evil stage drops
growing_epic_buff = true#                        # makes the extra stage always appear
sol_xp_buff = true#
weekend_stage_buff = true#
megablitz_buff = true#
colloseum_buff = true#
merciless_xp_buff = true#
material_stage_buff = true#
proving_grounds_buff = true#
hate_metal_hippoe_buff = true#
siege_buff = true#
facing_danger_buff = true#
orb_stage_buff = true#almost done                           # increases drops of island and enigma orb stages, using any of the three following options forces this one on
relic_aku_in_island = true#this is the part that isnt finished                      # adds relic and aku orb stages to island of hidden treasure
orb_stage_has_strong_massive_resist = true#      # makes it reasonable to get trait targetted ability orbs from non baron orb stages
orb_stage_has_ability_orb = true#                # adds white ability orbs to orb stages (on metal)



################# MISC CHANGES #################

[funny]
randomize_boss = false#                              # randomizes what enemy is boss in every stage
youcan_warning = true
special_surprise = true